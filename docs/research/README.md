# BudsMonitor Research Notes

This folder preserves the feasibility evidence that informs BudsMonitor for Windows.

The production app starts from the BLE-only findings in the legacy LibrePods Windows fork, but new implementation work should use the BudsMonitor solution under `src/`.
