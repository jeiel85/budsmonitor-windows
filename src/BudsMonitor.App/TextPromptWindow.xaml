<Window x:Class="BudsMonitor.App.TextPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BudsMonitor.App"
        mc:Ignorable="d"
        Title="입력" Width="380" SizeToContent="Height"
        ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource CanvasSoftBrush}">
    <StackPanel Margin="22">
        <TextBlock x:Name="MessageText" Style="{StaticResource Caption}" TextWrapping="Wrap" Margin="0,0,0,12" />
        <TextBox x:Name="InputBox" Style="{StaticResource AppTextBox}" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="취소" Style="{StaticResource ButtonUtility}" Margin="0,0,8,0"
                    Click="OnCancel" />
            <Button Content="확인" Style="{StaticResource ButtonPrimary}" IsDefault="True" Click="OnOk" />
        </StackPanel>
    </StackPanel>
</Window>
